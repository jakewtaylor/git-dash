name: CI

on:
  push:
    branches: master

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      CI: true
    strategy:
      matrix:
        node-version: [10.x]
    steps:
      - name: Deploy
        run: yarn deploy
